#include<stdio.h>
#include<math.h>
int main(){
    int n,p,sum=0,i;
    scanf("%d %d",&n,&p);
    for(i=1;i<=1;i++){
        sum=sum+(pow(n,p));
    }
    printf("%d",sum);
    return 0;

}